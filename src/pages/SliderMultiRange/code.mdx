```jsx
import { SliderMultiRange } from '@/components';

<>
  //horizontal
  <div
    style={{
      width: '100%',
      height: '350px',
      border: '1px solid red',
    }}>
    <SliderMultiRange
      sliderValues={sliderValues}
      colors={['red', 'yellow', 'green']}
      orientation="horizontal"
      onChange={setSliderValues}
    />
  </div>
  //vertical
  <div
    style={{
      width: '100%',
      height: '350px',
      border: '1px solid red',
    }}>
    <SliderMultiRange
      sliderValues={sliderValues}
      colors={['red', 'yellow', 'green']}
      orientation="vertical"
      onChange={setSliderValues}
    />
  </div>
</>;
```
